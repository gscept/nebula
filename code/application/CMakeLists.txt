#-------------------------------------------------------------------------------
# Game
#-------------------------------------------------------------------------------

fips_begin_module(application)
	fips_include_directories(${CMAKE_CURRENT_SOURCE_DIR} ${CODE_ROOT}/foundation)	
	fips_deps(foundation)
	fips_dir(.)
		fips_files(
			stdneb.h
			stdneb.cc
		)
	fips_dir(game)
		fips_files(
			entity.h
			entity.cc
			entityattr.h
			entityattr.cc
			featureunit.h
			featureunit.cc
			gameserver.h
			gameserver.cc
			manager.h
			manager.cc
		)
	fips_dir(game/component)
		fips_files(
			basecomponent.h
			basecomponent.cc
			componentdata.h
			componentdata.cc
			componentplugin.h
			componentplugin.cc
		)
	fips_dir(game/attr)
		fips_files(
			accessmode.h
			attrexithandler.h
			attrexithandler.cc
			attributedefinition.h
			attributedefinitionbase.h
			attributedefinitionbase.cc
			attrid.h
			attrid.cc
			valuetype.h
		)
	fips_dir(basegamefeature)
		fips_files(
			basegamefeatureunit.h
			basegamefeatureunit.cc
		)
	fips_dir(basegamefeature/managers)
		fips_files(
			entitymanager.h
			entitymanager.cc
			componentmanager.h
			componentmanager.cc
		)
	fips_dir(basegamefeature/components)
		fips_files(
			transformcomponent.h
			transformcomponent.cc
		)
	
	fips_dir(basegamefeature/loader)
		fips_files(
			blueprintloader.h
			blueprintloader.cc
			entityloaderbase.h
			entityloaderbase.cc
			levelloader.h
			levelloader.cc
			loaderserver.h
			loaderserver.cc
			userprofile.h
			userprofile.cc
		)
	fips_dir(graphicsfeature/components)
		fips_generate(FROM lightcomponentbase.nidl TYPE NIDL)
		fips_files(
			pointlightcomponent.h
			pointlightcomponent.cc
		)
	fips_dir(appgame)
		fips_files(
			gameapplication.h
			gameapplication.cc
		)
	fips_dir(.)	
fips_end_module()
