#-------------------------------------------------------------------------------
# benchmarks
#-------------------------------------------------------------------------------

fips_ide_group(benchmarks)
include_directories(.)
add_subdirectory(benchmarkbase)
add_subdirectory(benchmarkfoundation)